import Head from 'next/head';
import React from 'react';

const Home = () => {
  return (
    <React.Fragment>
      <Head>
        <title>Settings - Nextron (with-typescript)</title>
      </Head>
      <div>⚡ SETTINGS ⚡</div>
    </React.Fragment>
  );
};

export default Home;
